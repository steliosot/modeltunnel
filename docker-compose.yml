services:
  modeltunnel:
    build: .
    container_name: modeltunnel-app
    ports:
      - "8080:8080"
    volumes:
      - config_data:/home/appuser/.config/modeltunnel
      - ./config.docker.yaml:/home/appuser/.config/modeltunnel/config.yaml:ro
    restart: unless-stopped
    command: ["up"]

volumes:
  config_data:
